# Database Connection
# For local development with SQLite:
# DATABASE_URL="file:./dev.db"

# For production with PostgreSQL (Render):
DATABASE_URL="postgresql://user:password@host.virginia-postgres.render.com/database_name"
